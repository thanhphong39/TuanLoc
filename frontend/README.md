
  # Recreate UI from reference

  This is a code bundle for Recreate UI from reference. The original project is available at https://www.figma.com/design/FCmcWvTHFQf6zlahlZotz1/Recreate-UI-from-reference.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  